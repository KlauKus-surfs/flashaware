{
  "name": "flashaware-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end browser tests for FlashAware (Playwright). Boots against a running API + DB.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/bcrypt": "^5.0.2",
    "@types/node": "^20.11.5",
    "@types/pg": "^8.10.9",
    "bcrypt": "^5.1.1",
    "pg": "^8.11.3",
    "typescript": "^5.3.3"
  }
}
